import React from 'react';

const Error = () => {
	return(
		<div>
			<h2>Error Occured</h2>
			<p>If user occured any route error then render 404 page not found or occured any server error then render 500 internal server error</p>
		</div>
	)
}

export default Error;